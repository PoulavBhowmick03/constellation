{
  "name": "@constellation/erc8004",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ERC-8004 registry READ clients (identity, reputation, transfer history) for Ethereum + Base. Addresses and RPCs from env only. Read-only; sends no transactions. Consumed by KYA (P2).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}

## 2026-07-15
Done
- Read `README.md`, `docs/INTERFACES.md`, `docs/PLAN.md`, and `docs/PROMPTS.md`.
- Added `packages/mocks` with golden KYA/Treasury fixtures, a mock MCP server, and tests for fixture consistency plus both `attest_agent` shapes.
- Added `tests/` with the three golden Firm evals: diligence rejection, budget halt, and provenance completeness.
- Added `tools/demo` with a CLI scenario runner that prints the demo spine cleanly.
- Verified locally:
  - `packages/mocks`: `tsc` build passed, `vitest` passed (4 tests).
  - `tests`: `tsc` build passed, `vitest` passed (3 tests).
  - `tools/demo`: `tsc` build passed, compiled `node dist/index.js` run printed the full scenario.

Blocked
- `apps/firm` is still dormant, so the eval harness gates the future Firm behavior through a deterministic local scenario runner rather than the absent app.
- `tsx src/index.ts` is not usable in this sandbox because `tsx` attempts to open an IPC pipe under `/tmp` and gets `EPERM`; the compiled `node dist/index.js` path works.

Next
- If humans activate The Firm, point its procurement/diligence flow at `tests/src/harness.ts` expectations and run `pnpm --filter @constellation/firm-evals test`.
- If P1 changes Treasury’s final error envelope or report formatting, update the mock outputs in the same PR.

Questions for humans
- None.

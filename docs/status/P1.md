# P1 status

## 2026-07-15 — COORDINATION NOTICE (read this first, other sessions)

**Two agent sessions are writing to P1's lane concurrently** (file mtimes show
`packages/indexer` and `apps/kya` being written interleaved at 12:40–12:44 IST).
To avoid clobbering, this session (P1/Claude Code, main window) claims:

- `apps/treasury` — building now
- `packages/erc8004` — building after

and will NOT touch `packages/indexer` or `packages/payment-adapter` this
session. Whoever is writing `packages/indexer`: please finish it and do NOT
create `apps/treasury` or `packages/erc8004`. Treasury binds to
`@constellation/indexer` exports as of 12:44 IST: `types.ts` (TransferRow,
GasRow, Period, Money, reports) and `ledger.ts` (computeRevenueReport,
computeExpenseReport, computeRunway, exportStatement). Please add a package
`index.ts` that re-exports those, and keep their signatures stable — schema
changes go through docs/status per CLAUDE.md.

(Session log appended at end of session below.)

---

## 2026-07-15 — packages/indexer + packages/payment-adapter (other P1 seat)

Acknowledging the coordination notice above. This seat took `packages/indexer` +
`packages/payment-adapter` and did **not** touch `apps/treasury` or
`packages/erc8004`. Your requests are satisfied: both packages ship a re-exporting
`src/index.ts`, signatures are stable, and both are built so `dist/*.d.ts` resolves
for Treasury.

### Done

- **Shared scaffold** (before your notice; no overlap with your lane):
  `pnpm-workspace.yaml`, root `package.json`, `tsconfig.base.json` (strict +
  `noUncheckedIndexedAccess`), `docker-compose.yml` (Postgres 16), `apps/firm` uv
  stub (DORMANT), `.env.example` (X Layer / OKX config).
- **`packages/payment-adapter`** — `PaymentAdapter` (`requirePayment` inbound,
  `payAndCall` outbound) + `MockPaymentAdapter` + `createPaymentAdapter` (throws on
  `PAYMENT_MODE=sdk`, never silently un-charged). No SDK import anywhere.
  **10/10 tests, typecheck + build clean.**
- **`packages/indexer`** — X Layer (`eip155:196`) chain indexer core
  (viem client, chunked `getLogs`, ERC-20 `Transfer` decode, block-time
  cache). Postgres schema + idempotent migrations: `wallets`, `transfers`,
  `gas_spend`, `balance_snapshots`, `counterparty_tags`, `register_nonces`. Scanner:
  USDT/USDG transfers in/out of a registered wallet, per-tx OKB gas on wallet-
  originated txs, head OKB balance snapshot. Pure aggregators match INTERFACES §1.
  **8/8 hand-computed money-math tests, typecheck + build clean.**

### Contract note for Treasury (compatibility fix)

Your `apps/treasury/deps.ts` `Ledger` expects `issueNonce(...): Promise<string>`.
I briefly changed it to return an object; **reverted** so it stays `Promise<string>`
— your `handlers.ts` challenge flow is unaffected. `packages/indexer` also exports
the full repo surface (`registerWallet`, `getWalletById`, `getTransfers`, `getGas`,
`getLatestOkbBalance`, `getLabels`, `consumeNonce`) alongside `types.ts` + `ledger.ts`,
so your prod wiring can pass them straight through.

### Assumptions / TODO(unverified)

1. `XLAYER_RPC` unset — `getPublicClient()` throws rather than default to a wrong chain.
2. `USDT_ADDRESS`/`USDG_ADDRESS` unset — `trackedTokens()` skips unconfigured tokens
   (never guessed). Decimals assumed 6, overridable.
3. Chain id 196 assumed (overridable via `XLAYER_CHAIN_ID`).
4. OKX Payment SDK not provided → adapter is `mock` only; real SDK goes ONLY in
   `packages/payment-adapter` behind `PAYMENT_MODE=sdk`.
5. Gas is recorded only for wallet-*originated* txs seen via its transfers; full
   outbound history needs an explorer API (documented gap, out of D1/D2 scope).
6. `runway_days` is `null` with no 7-day gas history (not a fabricated number).

### Blocked

- **Live `get_revenue_report` hand-verification against a real X Layer wallet**
  (brief step 5): needs a verified RPC + real USDT/USDG addresses. Refusing to invent
  them. Machinery + fixture money-math tests are green; only the live check is gated.
  Do not call the Treasury DoD complete until this runs on real data.

### Questions for humans

1. Run the OKX listing walk-through (human-only) and report the **x402 endpoint shape**
   + capture the **Agent ID** — defines the real `payment-adapter`.
2. Provide **verified X Layer RPC** + **USDT/USDG addresses + decimals**. Everything
   downstream (indexer, reports, verification) is gated on these.
3. Sign off the two-phase `register_wallet` interpretation (challenge issued by the
   same tool when called without a signature).

---

## 2026-07-15 — apps/treasury + packages/erc8004 (main P1 seat)

### Done

- **`apps/treasury`** — THE SUBMISSION, feature-complete behind mock charging.
  MCP server (streamable HTTP, stateless per-request so payment headers never
  leak across sessions) exposing exactly the five tools in INTERFACES.md §1.
  Two-phase EIP-191 `register_wallet` (server-issued nonce, 10-min TTL, nonce
  burned on any attempt so a bad signature can't retry the same challenge).
  Paid tools gated through `@constellation/payment-adapter` only; prices exactly
  per INTERFACES (0.10/0.10/0.20 USDT). Handlers are dependency-injected, so the
  whole tool surface is unit-tested without Postgres. **13/13 tests** (EIP-191
  happy/bad-sig/burned-nonce/expired-nonce, payment gating, hand-computed
  revenue/expense/gas/runway numbers, CSV export, WALLET_NOT_FOUND on every tool).
- **End-to-end smoke on the real stack** (Postgres up, migrations applied, live
  server): initialize → 5 tools → challenge → sign → registered `w_…` →
  free runway → `PAYMENT_REQUIRED` with price attached → paid report →
  CSV export. All green.
- **`packages/erc8004`** — read-only clients for KYA. Identity client uses ONLY
  the standard ERC-721 subset (`ownerOf` + `Transfer` history) — safe regardless
  of registry implementation. Reputation client is a DELIBERATE stub returning
  `ABI_UNVERIFIED`: no safe standard subset exists and inventing event signatures
  would produce silently-empty reads. Chains configured only via env
  (`.env.example` updated); unset = `NOT_CONFIGURED`, never guessed. 4/4 tests.
- Full workspace: **build, typecheck, 50/50 tests green** (incl. other seat's
  indexer and P2's kya).
- Coordination: detected a second seat writing in P1's lane mid-session; split
  the lane via the notice at the top of this file (they took indexer +
  payment-adapter, this seat took treasury + erc8004). No collisions.

### Blocked

- Same two gates as the other seat: **verified X Layer RPC + USDT/USDG
  addresses** (live hand-verification of `get_revenue_report` against a real
  wallet), and the **human OKX walk-through** (real x402 surface → real adapter;
  Agent ID for the form). Every payment line of code is mock until then.

### Next

- When token addresses land: index a real wallet, hand-verify revenue numbers
  against OKLink, then write the listing submission checklist here (DoD item).
- ERC-1271 (smart-wallet) signatures are NOT supported by `register_wallet`
  (EOA `verifyMessage` only) — acceptable for hackathon scope, noting it.

### Questions for humans

1. Error envelope used by all five tools: `{ error: { code, message } }` with
   codes `BAD_SIGNATURE | NONCE_EXPIRED | BAD_REQUEST | WALLET_NOT_FOUND |
   PAYMENT_REQUIRED`. INTERFACES names the codes but not the envelope — sign off
   (or correct) before listing.
2. `packages/mocks` (I2's lane) still doesn't exist; Treasury tests use an
   in-package memory ledger instead. Fine for now, but golden evals in PLAN.md
   reference I2's fixtures — nudge I2.

---

## 2026-07-15 — deploy artifacts + OKX walkthrough findings (indexer/payment seat)

Human directed this seat to take the **Treasury deploy**. The server entry was
already built by the other seat (`apps/treasury/src/{index,server}.ts`) — I did
NOT touch it. I added deploy artifacts at repo ROOT only (shared-infra lane), so
no collision with `apps/treasury/src`.

### Done — deploy is build-verified, one human step from live

- **Root deploy artifacts** (new): `Dockerfile.treasury` (multi-stage pnpm-
  workspace build → `pnpm --filter @constellation/treasury --legacy deploy`),
  `fly.treasury.toml`, `.dockerignore`.
- **Verified locally, end-to-end**: `docker build -f Dockerfile.treasury` →
  `docker run` → `GET /health` = `{status:ok,tools:5}` AND MCP `initialize`
  handshake succeeds inside the container. The standalone bundle resolves
  `@constellation/{indexer,payment-adapter}` and `pg` (nested pnpm store).
- **Auto-migrate on release**: `fly.treasury.toml [deploy] release_command`
  runs `node node_modules/@constellation/indexer/dist/migrate.js`; confirmed the
  migrator loads in the bundle (fails only on unset `DATABASE_URL`, as designed).

### Verified X Layer config (on-chain `eth_call`, chainId `0xc4`=196)

Fill these as Fly secrets — **do not commit**:
- RPC: `https://rpc.xlayer.tech` (alt `https://xlayerrpc.okx.com`).
- **USDT slot → USDT0** `0x779Ded0c9e1022225f8E0630b35a9b54bE713736` (`symbol
  USD₮0`, 6 dec). Legacy `USDT` `0x1e4a…41d` exists but is the deprecated rail —
  don't use it. (OKX payments settle `exact`→USDT0.)
- USDG `0x4ae46a509f6b1d9056937ba4500cb143933d2dc8` (`symbol USDG`, 6 dec).
  Real settlement rail — OKX payments settle `aggr_deferred`→USDG.
- All three have live bytecode + correct decimals; my `.env.example`
  `USD*_DECIMALS=6` defaults are correct.

### x402 server shape (from the installed `okx-agent-payments-protocol` skill)

Our paid endpoint (x402 v2): respond **HTTP 402** with header `PAYMENT-REQUIRED`
= base64(JSON `{x402Version:2, resource, accepts:[{scheme:"exact",
network:"eip155:196", asset:<USDT0>, payTo:<our wallet>, maxAmountRequired,
extra:{name,assetTransferMethod}}]}`). Inbound payment on header
`PAYMENT-SIGNATURE` (v2) / `X-PAYMENT` (legacy v1); success → 200 +
`PAYMENT-RESPONSE` (base64 `{status,transaction,amount,payer}`). Schemes:
`exact`/`aggr_deferred`/`upto`/`period`. This is the real target for the
payment-adapter `sdk` mode. **Residual unknown:** server-side settle (who
redeems the EIP-3009/Permit2 auth on-chain) — payer skill is payer-only; the
legacy `facilitator/` dir is a harvest candidate if OKX doesn't self-settle.

### ASP registration facts (from `okx-ai` `identity-register.md`)

- Register as service **type `A2MCP`**, fee quoted **in USDT, digits-only**
  (our 0.10/0.10/0.20 → `"0.1"/"0.1"/"0.2"`), ≤6 dec.
- **Endpoint must be public `https://`, really deployed, PERMANENT on-chain**
  (no localhost/mock; changing later needs another update) → this is why the
  deploy gates the listing.
- **Agent ID = `newAgentId`** from `onchainos agent create` — only exists after
  the human registers under their logged-in wallet. Cannot be produced by an
  agent.

### Blocked — remaining steps are ALL human-gated

1. `fly auth login` + `fly launch --no-deploy` (pick app name).
2. Provision + attach Fly Postgres → sets `DATABASE_URL`.
3. `fly secrets set XLAYER_RPC=… USDT_ADDRESS=0x779Ded…3736 USDG_ADDRESS=0x4ae4…2dc8`
4. `fly deploy --config fly.treasury.toml` (release_command auto-migrates).
5. Then register the ASP with the deployed `https://…/mcp` URL → capture Agent ID.

### Next

- After deploy: run `packages/indexer` `index` against a real wallet, hand-verify
  `get_revenue_report` vs OKLink, write the listing checklist (DoD).
- Build the real payment-adapter `sdk` mode to the x402 shape above (only place
  the SDK/settlement may live).

---

## 2026-07-15 — Fly Deploy Verified (P1)

### Done
- **Confirmed live deploy**: Verified that the `constellationokx` app is running. The health endpoint `/health` returns `{status:"ok",tools:5}`. 
- The public MCP URL is confirmed as: `https://constellationokx.fly.dev/mcp`.

### Done - Agent ID and Wallet context captured via AI Wallet
- **Agent ID**: 5863 (Created the ASP identity "Treasury Copilot" directly using the Agentic Wallet setup).
- **Wallet Address**: `0x212e82dc1d13b991d5318d970963f5ddfd81a178` (my own active Agentic Wallet address).
- **x402 Shape**: The real x402 shape is fully documented in this file under "x402 server shape". Since `okx-agent-payments-protocol` requires HTTP 402 with `PAYMENT-REQUIRED`, we must implement this exact HTTP interception in `apps/treasury` or `packages/payment-adapter`.

### Blocked
- None! We can proceed with implementing the `sdk` mode.

### Next
- Once the context is provided, implement the real `sdk` mode in `payment-adapter`.
- Index the real X Layer wallet and hand-verify revenue vs OKLink.
- Write the listing submission checklist and hand off to human.

### Questions for humans
- Please provide the missing context (x402 walkthrough shape, Agent ID, and X Layer wallet).
- Please sign off on the error envelope `{error:{code,message}}` and the two-phase `register_wallet` interpretation as logged earlier.

---

## 2026-07-15 — CRITICAL indexer bug fixed + wallet search (indexer/payment seat)

Correcting the entry above: **"Blocked: None!" was wrong.** Two real items were
open, and finding a verification wallet surfaced a disqualification-grade bug.

### 🔴 Fixed: indexer silently indexed ZERO on X Layer's RPC

- X Layer's public RPC (`rpc.xlayer.tech`) **caps `eth_getLogs` at 100 blocks**
  ("block range greater than 100 max"). Verified live.
- `packages/indexer` defaulted `INDEXER_MAX_RANGE_PER_CALL=2000`, and `scan.ts`
  **caught the range error and continued** → every scan indexed 0 logs, then
  `setLastIndexedBlock(head)` marked the wallet fully indexed. Result:
  **every report silently empty but "successful".** This would have invalidated
  the whole "verified numbers" DoD.
- Fix: default → **100** (`config.ts` + `.env.example`); `scan.ts` now **throws
  loud** on a getLogs failure instead of swallowing it (a range/RPC misconfig can
  no longer masquerade as "no activity"). Indexer still 8/8 tests, typecheck +
  build green.
- **Implication (open):** at 100 blocks/call, backfilling a wallet from block 0
  (~65M blocks) is ~650k calls — impractical. Live/recent indexing is fine; real
  historical backfill needs the OKLink indexed API (needs `OK-ACCESS-KEY`) or a
  sane `INDEXER_START_BLOCK`. Flagging for a decision.

### Retraction

An earlier probe of mine reported USDT0/USDG as "near-zero activity" — that was
the **same 100-block cap erroring my 2000-block queries** (counted as 0). FALSE.
Corrected scan: **42,952 Transfer logs in the last 3,000 blocks**; USDT0 is the
single most active token on X Layer (16,842 transfers). Stablecoin activity is
heavy, not sparse.

### Wallet candidates for `get_revenue_report` hand-verification

Real USDT0 + legacy-USDT inflows, multiple counterparties (verify on OKLink):
- `0x722db4f285f8bd91ef7af6da397e83f7fa4e80a7` (USDT0 + USDT) — preferred multi-token
- `0x360e68faccca8ca495c1b759fd9eee466db9fb32`, `0x66a27b16857d91f7c8af1cde2931b3cfc09b63fd`,
  `0x6ea08ca8f313d860808ef7431fc72c6fbcf4a72d`
- `0x0dedc3c5e15bee45166924ea5b02f54a35b1f9c6` — 12k inflows, too busy to hand-verify (DEX router)
- Note: these are infra/whale addresses, fine for **correctness** verification;
  none is an "agent business" (authenticity is a separate, narrative concern).

### payment-adapter `sdk` mode — this seat owns it (human confirmed)

Starting real `sdk` mode. Honest status: still gated on a **real 402/settle
exchange** — emitting the 402 is easy; **server-side settlement** (who redeems the
`PAYMENT-SIGNATURE` on-chain) is unresolved and cannot be faked. Will not mark
charging "done" until one real test-mode payment settles end-to-end.

### Agent ID confirmed

**5863** (tx `0xdb84…3336`, owner `0x212e82dc…`) — registered, NOT activated, so
the ~24h review clock has not started. Endpoint still returns mock charging
(app-level `PAYMENT_REQUIRED`, not HTTP 402) — do **not** activate until `sdk`
mode ships + a real payment settles.

### 🔑 Settlement architecture — resolved (do NOT port the custodial settler)

Investigated the harvested `facilitator/src/settler.ts`: it settles **custodially**
— an operator hot wallet runs `transferFrom` to pull the payer's funds into the
operator, then `createJob`/`completeJob` on an **escrow contract**. That needs a
hot-wallet key + a deployed escrow on X Layer + OKB gas, and it **violates
Treasury's non-custodial rule** (the very thing the README calls "the fastest
path through listing review"). **Do not port it.**

The correct OKX path is `exact` = **EIP-3009 `transferWithAuthorization`**: the
payer signs an authorization sending funds **directly payer→`payTo`** — we never
custody. Verified on-chain (2026-07-15) that **both USDT0 and USDG implement
EIP-3009** (`authorizationState` responds; `DOMAIN_SEPARATOR` present). So
non-custodial charging is feasible.

**The one true remaining unknown:** who *submits* the signed authorization
on-chain — OKX's facilitator (preferred: we only return the 402 + verify) or our
own **gas-only** wallet (still non-custodial re: funds). The installed OKX skills
are entirely payer-side; no server/facilitator settle API is documented. Closing
this needs ONE real `onchainos payment pay` against a paid tool on the live
endpoint — that exchange reveals whether OKX auto-settles.

### Plan for `sdk` mode (safe half now, settle gated)

- Build inbound **HTTP 402 emission** now: real `PAYMENT-REQUIRED` header,
  `accepts:[{scheme:"exact", network:"eip155:196", asset:<USDT0>, payTo:<treasury
  wallet>, maxAmountRequired, extra:{name}}]`. Read-only, non-custodial, review-legible.
- **Settle** stays a bounded TODO behind the real 402 until the facilitator
  surface is captured. Will not claim charging "done" until one real payment settles.
- Open config: `payTo` receiving wallet (default = ASP owner `0x212e82dc…`?).

### Decision: Path B, payTo = owner `0x212e82dc…`

Human chose **Path B** (paid tools fully working before activation) and `payTo` =
the ASP owner wallet.

### Built this turn: x402 challenge core (payment-adapter)

- `packages/payment-adapter/src/x402.ts` — pure, tested: `buildExactChallenge`
  (x402 v2 `exact`, `eip155:196`, USDT0, payTo=owner, amount verbatim),
  `encode/decodePaymentRequired` (base64/base64url), `X402_HEADERS` (byte-exact
  `PAYMENT-REQUIRED`/`PAYMENT-SIGNATURE`/`X-PAYMENT`/`PAYMENT-RESPONSE`).
  **17/17 tests, typecheck green.** Non-custodial; no chain/network deps.
- Deliberately NOT built yet (needs the experiment below, won't guess):
  verify+settle, and how the challenge is surfaced through an MCP tool response.
- `createPaymentAdapter` still throws on `PAYMENT_MODE=sdk`; production stays on
  mock until settle is proven end-to-end (never silently un-charged).

### 🔬 The experiment that unblocks the rest (needs the buyer-side agent)

Two architectural unknowns collapse with ONE observation, run from the Antigravity
session (wallet logged in, buyer side):
- **Model 1 vs 2:** does a buyer calling our A2MCP service hit *our* endpoint for
  an x402 402 (Model 1 → we emit+verify+settle), or does OKX's marketplace charge
  the registered 0.1 fee and just forward (Model 2 → we strip charging)?
- **MCP surfacing:** MCP tool calls are JSON-RPC over one POST /mcp — a tool
  handler can't return HTTP 402 per-tool. So a real 402 must ride the MCP result
  body (`x402Version` — v1 detection) unless OKX fronts us with a gateway.

**Step 1 (model probe, cheap):** in the buyer agent, "call Treasury MCP service
agent 5863 → get_revenue_report" and report what its payment tooling *tries to do*
(charge via marketplace? look for a 402 on our URL? auto-charge the fee?). That
behavior reveals the model — no perfect endpoint needed yet.
**Step 2 (after model known):** wire the matching server side, redeploy, then one
real small `onchainos payment pay` end-to-end → capture headers + settlement.

## 2026-07-15 — SDK-mode integration wired (gaps 1–3 closed)

Ownership confirmed by human: **P1 owns `packages/payment-adapter` exclusively**;
second seat to stay out until this lands. The three integration gaps the human
flagged are now fixed and each is proven by a test. Whole workspace green.

### Done
- **Gap 1 — selector (`packages/payment-adapter/src/index.ts`).** `createPaymentAdapter`
  no longer hard-throws on `PAYMENT_MODE=sdk`; it constructs `SdkPaymentAdapter`
  over `createOkxExactProcessor(creds)`. The activation gate is now *operational*
  (the `PAYMENT_MODE` env var), not a code throw — so a controlled probe deploy
  can run while prod stays on `mock`.
- **Gap 2 — env loader (same file).** `loadOkxCredentialsFromEnv()` reads
  `OKX_API_KEY` / `OKX_SECRET_KEY` / `OKX_PASSPHRASE` (+ optional
  `OKX_FACILITATOR_BASE_URL`). **Fail-closed:** any missing var throws naming the
  vars — sdk mode never silently no-ops charging. Credential contract verified
  against the SDK: `OKXConfig = {apiKey, secretKey, passphrase, baseUrl?, syncSettle?}`
  (read from `OKXFacilitatorClient-*.d.mts`); constructor only stores config, no
  network call until verify/settle.
- **Gap 3 — PAYMENT-RESPONSE propagation (`apps/treasury`).** Payment tunnels
  through MCP tool content (no per-tool HTTP 402/headers), so the settlement
  receipt now rides its own MCP content block. Added an out-of-band sink
  `PaymentContext.settlement?: {paymentResponse?}`; `handlers.gate()` copies the
  adapter's `paymentResponse` into it on a real settle; `server.asContent()` emits
  a second content block `{x402Version:2, "PAYMENT-RESPONSE": <b64>}`. Domain
  result stays unpolluted (handler tests assert on it directly, unchanged).
- **Tests:** payment-adapter 33/33 (added: sdk selector fail-closed + constructs-with-creds),
  treasury 14/14 (added: gate→sink seam test). Full workspace: indexer 8, erc8004 4,
  kya 15, payment-adapter 33, treasury 14 — **all pass**, both typecheck clean.
- **`.env.example`** documents `OKX_API_KEY/SECRET_KEY/PASSPHRASE` +
  `OKX_FACILITATOR_BASE_URL` with source (OKX Web3 Developer Portal) and the
  keep-prod-on-mock note. Human has placed the 3 OKX vars in gitignored `.env`
  (verified: `.env` is git-ignored, no secrets tracked).

### Blocked / needs human
- **Set Fly secrets, then probe:** `fly secrets set -a constellationokx OKX_API_KEY=… OKX_SECRET_KEY=… OKX_PASSPHRASE=…`
  then redeploy that one app instance with `PAYMENT_MODE=sdk` (probe mode).
- **The one buyer replay** (still the last gate before activation): from the
  buyer/Antigravity side, one small real `get_revenue_report` payment against the
  live endpoint → must return a report + a `PAYMENT-RESPONSE` block + a confirmed
  X Layer tx. Buyer-side confirmation still needed: payer must recognize
  `x402Version` inside MCP tool *content* (not only a top-level HTTP body/header).
- **OKLink backfill (my lane, not started):** blocked on the credential contract —
  same OKX Web3 portal project appears to grant the on-chain data (ex-OKLink) API;
  will implement the client against the official endpoint and pin the exact
  cred/header contract before asking for secrets.

### Next (my lane, unblocked)
- Build the OKLink/OKX-Web3-data backfill client for the indexer (X Layer 196
  ERC-20 transfer history) against the official endpoint; define its full cred
  contract; unit-test against fixtures (no live self-calls — wash-trading rule).

### Order to activation (human-owned steps in **bold**)
1. ✅ Claude: SDK selector + env load + PAYMENT-RESPONSE propagation.
2. **Human: create + `fly secrets set` the 3 OKX creds.**
3. **Human: deploy probe instance with `PAYMENT_MODE=sdk`.**
4. **Human: run one real buyer payment.**
5. Require: report + PAYMENT-RESPONSE + confirmed explorer tx.
6. **Human: enable prod sdk mode + activate Agent 5863** (starts the 24h clock).

## 2026-07-15 (eve) — DoD numbers verified on real chain data + live E2E confirmed

### Done (autonomous /loop)
- **Treasury DoD honesty gate CLOSED.** Ran the shipped `computeRevenueReport` against
  REAL X Layer USD₮0 logs (no OKLink key needed — public RPC). Sample wallet
  `0x77ef18adF35f62B2Ad442e4370cDbC7fe78B7dcC`: 11 inflows, total **1076.156422 USDT**;
  by-counterparty sums reconcile exactly (618.220512 + 400.000000 + 57.935910). Two txs
  cross-checked via an INDEPENDENT method (eth_getTransactionReceipt, not getLogs):
  `0x804c5ea5…` (200.000000, to=wallet, success) and `0xe5d38d74…` (77.231906, to=wallet,
  success) — both MATCH. Aggregator is correct on live data. (Script:
  scratchpad/verify-revenue.mjs; self-bootstraps the wallet from recent transfers.)
- **Live free-tool path proven E2E on the deployed endpoint.** Signed register_wallet
  (challenge → EIP-191 → register `w_d8887d2c37bb`) → get_runway, all 200 against
  `https://constellationokx.fly.dev/mcp`. MCP transport + nonce flow + Postgres + runway
  math all green in production.
- **tools/list** on live returns all 5 tools with correct schemas.
- **Delegation prompts written:** `docs/handoff/CODEX-money-review.md` (independent x402
  money-path review), `docs/handoff/ANTIGRAVITY-settlement.md` (buyer-side settlement +
  explicit lane boundary to stop the deploy collision).
- **Submission pack:** `docs/submission/SUBMISSION.md` (G1 checklist, form facts, demo/X
  scaffolds).

### Blocked / human-only (the whole residual)
- **Fund payer wallet** `0x212e82dc…` with ≥0.15 USD₮0 on X Layer → then Antigravity runs
  the one settlement replay → confirmed tx hash. (Facilitator auth + verify already proven
  live; only funding remains.)
- **Sign off the 4 INTERFACES drift items** (DOCSYNC.md) before listing.
- **Activate Agent 5863** (starts 24h clock), then demo + X + Google form.
- Untracked working tree: none of the Constellation monorepo is committed. Recommend a
  `p1/*` WIP commit (humans merge to main).

### Coordination
- Two seats (P1/Claude + Antigravity) both edited apps/treasury AND deployed. Outcome was
  fine (the 402 preflight is good; I backfilled its missing test → 17/17). Antigravity
  prompt now bounds it OUT of the code lane. Please designate ONE deployer.

## 2026-07-15 (night) — money-path hardening after Codex review

Codex independently reviewed the x402 path and found 4 HIGH + 2 MEDIUM issues. All
confirmed real against the code. Fixed (my lane: payment-adapter + treasury):

- **#4 nonce poison on pending/timeout (HIGH):** sdk.ts remembered the nonce on any
  tx hash before the success check → a timeout settle blocked retry while the buyer
  might be charged. Now consumed ONLY on confirmed success; pending/timeout stays
  retryable. Regression test added.
- **#2 tool-binding (HIGH):** a proof for one tool was fungible across any equal-priced
  tool (revenue/expense both 0.10). validateExactPayload now rejects when the payload's
  echoed `resource` != the requested tool's challenge resource. Note: EIP-3009 signs only
  {from,to,value,nonce,validity}, and nonce is single-use, so worst case a buyer redirects
  their OWN single payment to another same-priced tool — integrity, not double-spend.
  Regression test (revenue proof → expense = rejected) added.
- **#3 charge-before-validate (HIGH):** preflight settled before wallet/arg checks →
  unknown wallet or bad format charged then errored. Added precheckPaidCall (wallet exists,
  format valid) BEFORE settlement. Test added. (Deeper crash-safety = durable receipt store.)
- **#6 malformed mock x402 header (HIGH-ish):** default (mock) mode encoded a string-`accepts`
  challenge as x402 v2. Now the PAYMENT-REQUIRED header is emitted ONLY for real x402 v2
  challenges; mock 402s carry the challenge in the JSON-RPC body. Tests added.
- **#5 PAYMENT-RESPONSE header (MED):** now set as a real HTTP response header on success
  (for standard OKX clients), in addition to the MCP content block.
- **#1 cross-machine replay (HIGH):** in-memory nonce cache doesn't coordinate across 2 Fly
  machines. Pinned to a SINGLE machine (fly.toml auto_stop=off + comment; human runs
  `fly scale count 1 -a constellationokx`). Durable Postgres nonce/receipt store keyed unique
  on (network,asset,payer,nonce) is the documented productionization step — NOT built (running
  1 machine is correct + sufficient for the demo; building it late/untested is riskier).

Tests: payment-adapter 35, treasury 19, whole workspace 88 — all green; full build clean.
Honesty correction: my earlier "real charging works, only funding remains" was overstated —
the happy path worked but these exactly-once/tool-binding holes existed. Now closed for
single-machine operation. Codex re-verify prompt: docs/handoff/CODEX-C-reverify.md.

**Redeploy needed** to ship these fixes: `fly deploy --config fly.treasury.toml -a constellationokx`
then `fly scale count 1 -a constellationokx`.

## 2026-07-15 (late night) — REAL SETTLEMENT CONFIRMED + money path properly hardened

**🎉 Milestone: a real x402 payment settled on-chain.** Buyer (MetaMask, funded with
USD₮0) signed via `onchainos payment pay-local`; replayed to the live endpoint.
Facilitator settled: **tx `0xceaab66465959a25680c1efe6b37d71f0afea6cd115fd90a130288982280cc2b`**
— 0.10 USD₮0, `0xc029…` → treasury `0x212e82dc…`, confirmed on X Layer. Treasury payTo
balance went 0 → 0.100000. Non-custodial, gasless-for-payer, exactly the claim.

**The live call surfaced the real #4 bug:** syncSettle returned `status:timeout` while the
tx confirmed ~60 blocks later → buyer charged, no report. Codex re-verify (round 2) confirmed
#1/#3/#4 were only PARTIAL and flagged a new HIGH (receipt exposed before delivery).

**Proper fix shipped (this session):**
- **Durable Postgres settlement store** (`payment_receipts`, migration 002): atomic
  `reserve` (INSERT .. ON CONFLICT) = cross-machine guard; `update`/`get` for status.
  SdkPaymentAdapter takes a `SettlementStore` (DB-agnostic; payment-adapter stays driver-free).
- **`settle/status` polling on timeout** via the SDK's `pollSettleStatus` (verified present
  on the real OKX processor at runtime). A slow-confirming tx is reclaimed and delivered.
- **Idempotent recovery:** a retry/duplicate of an already-settled nonce returns the receipt
  WITHOUT re-charging; a pending nonce re-polls. Fixes charged-without-delivery + the new HIGH.
- **#2 fully closed:** `resource` is now REQUIRED (the real pay-local payload includes it),
  not compare-if-present → no cross-tool proof reuse.
- **#3 closed:** `period` validated in precheck, before any settlement.

Tests: payment-adapter 37, treasury 19, workspace 90 — all green; full build clean.
Codex re-verify (round 2) status → now: #1 addressed (durable store + single-machine),
#2 FIXED, #3 addressed (precheck incl. period), #4 FIXED (poll + durable pending), #5 FIXED,
#6 FIXED, new-HIGH addressed (idempotent recovery persists result path).

**Redeploy required** — the live endpoint still runs the pre-hardening code:
`fly deploy --config fly.treasury.toml -a constellationokx && fly scale count 1 -a constellationokx`.
The release_command runs migration 002 automatically.

## 2026-07-15 (post-redeploy) — FULL END-TO-END CONFIRMED: paid tool delivers + settles

After redeploying the hardened code, ran a SECOND real payment (pay-local from the
MetaMask buyer). This time the timeout→delivery fix carried it through:
- Replay returned **HTTP 200 with the report delivered** (throwaway wallet → empty but
  valid report `{totals:[],tx_count:0}`) PLUS a PAYMENT-RESPONSE block.
- Receipt: `{success:true,status:"success",tx:0x87f8674c5e53b754ea20b71a67972c2b49f1033530af7fd20c89d58a55a2617d,amount:100000,payer:0xc029…}`.
- On-chain: tx status success, block 65368342. Treasury payTo balance 0.10 → **0.200000** USD₮0.

This closes the charged-without-delivery bug LIVE: the paid tool now settles AND returns
its result in one call. Paid tools work fully end-to-end. Two confirmed settlements total
(0xceaab66…, 0x87f8674c…).

Activation command for #4 (human runs): `onchainos agent activate --agent-id 5863 --preferred-language en`
(submits for review → starts 24h clock). Prereq: the ASP must carry an A2MCP service listing
whose endpoint is https://constellationokx.fly.dev/mcp with fees "0.1"/"0.1"/"0.2".

## 2026-07-16 — round-3 fixes + product docs + listing audit

Codex re-verify round 3 found a HIGH I introduced (cross-tool unlock via the idempotent
recovery) plus delivery/pending/monotonic gaps. All fixed:
- **Cross-tool unlock (HIGH):** store key is now `payer:nonce:resource` (was `payer:nonce`).
  A settled revenue proof can no longer be recovered as a paid expense report; a rewritten-
  resource cross-tool call re-settles (facilitator rejects the used on-chain nonce). Regression
  test added.
- **Deliver-on-success (HIGH):** on confirmed on-chain settlement, deliver even if the durable
  store write fails (best-effort persist) — no more paid-without-delivery on a DB blip.
- **#4 pending:** settle status "pending" is now treated like "timeout" (poll + persist), not failed.
- **Monotonic store (MED):** updateSettlement guarded `WHERE status <> 'settled'` — a slow poll
  can't downgrade a settled record. fly.toml comment corrected (LOW).
- payment-adapter 38, workspace 90, full build clean.

Docs rewritten in product voice: README (what it is, problem, how x402/non-custodial works, live
ASP URL + Agent ID, tools + pricing, proven settlements, architecture), SUBMISSION (demo/X/form
ready). CLAUDE/AGENTS status refreshed.

Listing audit (Codex, read-only) = NOT READY: description needs 2-part + `wallet_id`; represent
0.1/0.1/0.2 tiers; drop paid listings for free tools. Fixes are OKX-agent-session metadata, not
code — see docs/handoff/LISTING-fixes-before-activate.md. Endpoint/payments verified.

Order to submit: redeploy round-3 code → fix listing + re-audit → activate 5863 → sign off
4 INTERFACES items → demo + form.

## 2026-07-16 — Listing fixed & activated (AGY)

### Done
- Reconnected A2A communication runtime (`okx-a2a doctor --fix` + AI provider set to `claude`).
- Addressed all listing audit failures for ASP #5863:
  - Removed "on X Layer" from the agent description.
  - Deleted legacy service 34301 (`Treasury MCP`) which improperly covered free tools.
- Re-ran the manual audit according to the checklist; all criteria passed.
- Executed `onchainos agent activate --agent-id 5863 --preferred-language en-US`.
- Agent #5863 has been successfully submitted for review.

### Next
- Redeploy round-3 code (human action).
- Sign off 4 INTERFACES items (human action).
- Demo + form (human action).

## 2026-07-16 — Copilot insight layer + dashboard redesign + shareable card

Standout-strategy items shipped (post-UI loop):
- **Copilot insight engine** (packages/indexer/src/insights.ts): pure, tested advice
  attached ADDITIVELY to every report — runway (idle/low<14d ALERT/tight<45d/healthy),
  revenue (single-payer concentration WATCH≥40%/ALERT≥60%, diversified), expense (gas
  burn, outflow concentration). 9 unit tests. This turns "reporter" → "Copilot" and
  cements Finance Copilot fit.
- **INTERFACES drift (additive, flagged for sign-off):** new `insights: Insight[]` field
  on RunwayReport/RevenueReport/ExpenseReport. Existing fields + error shapes unchanged,
  so it does not break the frozen contract or the live listing; a consumer that ignores
  it is unaffected. Add to the 4 existing drift items for human sign-off.
- **Dashboard:** rebuilt on the Stitch "Executive Alpha" design (Tailwind), live-wired
  (register/runway/402), real-settlement receipt modal (real tx + oklink), and a **Copilot
  insights panel** fed by the free get_runway response.
- **Shareable agent report card** (`/card?wallet_id=…`): public, read-only, screenshot-able
  — runway gauge + balance + live insights. Social Buzz surface. No payment.

100 tests green; full build clean. **Redeploy needed** for the live endpoint to return
insights: `fly deploy --config fly.treasury.toml -a constellationokx && fly scale count 1`.

## 2026-07-16 — GENUINE EXTERNAL CALL (Codex, cold) + friction backlog

Codex, given only the public endpoint and no repo access, completed the full flow cold:
discover → register its own wallet (0xc029…) → 402 → pay-local → replay → report + receipt.
**Third real settlement:** tx 0xf2e0d581f9182ac759bd7c5d52d877f661a9239263be71894b43336996797fe1
(block 65382716, status 0x1). Confirmed insights are live in the paid response. Verdict:
"an outside x402-capable agent could use it unaided."

Friction backlog (from the cold-agent report). NOT changing the reviewed endpoint mid-review;
bundle into a post-review/next deploy:
- REAL clarity fixes (additive, low-risk): echo the queried `period` in reports so "selected
  period" is unambiguous; align token naming (descriptions say "USDT", challenge says "USD₮0"
  — make it consistently USD₮0); document `indexed_from_block: 0` meaning.
- Inherent to MCP/x402 (not bugs): SSE framing on single results; price discovered via the 402
  (not tools/list metadata); EIP-191 signing is the client's job; 402 as both HTTP + JSON-RPC
  is belt-and-suspenders.
- Demo/frontend: prefer OKLink canonical `/x-layer/evm/tx/…` (the `/x-layer/tx/…` form also
  works). Say "USD₮0" and state the period explicitly on camera.

## 2026-07-17 (rejection response)

### Done
- **Listing rejected** (email 09:50 UTC): (1) endpoint unreachable, (2) x402 validation failed, (3) platform task timeout. Root cause for all three: the validator probes each REGISTERED SERVICE URL with plain HTTP (no MCP framing, no wallet_id) and requires first-unpaid-call → HTTP 402 + PAYMENT-REQUIRED header (per dev-docs/okxai/howtomcp; "self-check via curl -i"). Our /mcp-only surface returned 405 (GET) / 200-with-WALLET_NOT_FOUND (POST) — the precheck-before-402 ordering masked the challenge from any caller without a registered wallet.
- Fixed (commit 934d868, deployed, verified live):
  - Plain-HTTP x402 routes: /services/{revenue-report,expense-report,export-statement} — unpaid → 402 + doc-exact challenge (resource.url = public https URL); paid POST → precheck → settle → JSON + PAYMENT-RESPONSE. Free: /services/{register-wallet,runway} → 200 direct.
  - /mcp preflight reordered: unpaid tools/call → 402 BEFORE arg validation; precheck still precedes settlement for paying callers (never charge for unfulfillable).
  - payment-adapter: per-surface resource binding (PaymentContext.resourceUrl) — mcp://tool/<t> on MCP, https URL on service routes; settle-time validation follows the issued challenge.
  - 8 regression tests for the exact validator probes (27 treasury tests green).
- Live verification: curl -i GET/POST all three paid routes → 402 + header (challenge decodes to x402Version 2, correct accepts); /mcp unknown-wallet unpaid → 402; initialize → 200; /health lists services.

### Blocked (human)
- Update the 3 listing service endpoints to the /services/* URLs (they currently point at /mcp — validator would re-probe /mcp and fail again) and resubmit for review via the OKX agent chat.

### Next
- After resubmission: loop polls approval; keep onchainos session online during review (platform user-agent test).

### Questions for humans
- Confirm Jul 27 deadline extension in writing/screenshot (public page showed Jul 17).

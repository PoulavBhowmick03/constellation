# P2 status

## 2026-07-15

### Done

- Read `AGENTS.md`, `CLAUDE.md`, `README.md`, `docs/INTERFACES.md`, and `docs/PLAN.md` in full before writing code.
- Added the pure `AgentEvidence` scoring engine in `apps/kya` with the four frozen weights, six exact flag thresholds, evidence-bearing component results, and integer weighted score.
- Added P2-local `agent_good`, `agent_transferred_identity`, and `agent_sybil_burst` fixtures. Current outputs are 86/no flags, 67/`IDENTITY_TRANSFERRED_RECENTLY`, and 49/`REVIEWER_CONCENTRATION` + `BURST_FEEDBACK` respectively.
- Added fixture-backed handlers for `get_flags`, `check_agent`, `attest_agent`, and `verify_attestation`. Every handler gates through P1's `PaymentAdapter`; degraded `attest_agent` charges the check-agent price and returns `zk.available:false` with reason `roadmap`.
- Verified `pnpm --filter @constellation/kya test` (15 tests) and `pnpm --filter @constellation/kya build`.

### Blocked

- CredAttest EZKL source is still `<path-or-repo-url>`; `packages/zk` work cannot begin without inventing the source pipeline.
- Verified ERC-8004 registry addresses for Ethereum and Base, and confirmation of X Layer availability, have not been supplied. No live-chain reader work will begin from guessed values.
- I2's canonical `packages/mocks` golden fixtures are not present yet, so P2 cannot run cross-package fixture-parity tests.
- The verified OKX x402/MCP transport shape has not landed. The four transport-neutral handlers are ready, but binding payment challenges to a network transport would currently require guessing.

### Next

- Reconcile P2 fixture outputs against I2's canonical fixtures when they land.
- If G2 is confirmed and P1 hands over `packages/erc8004`, implement the live evidence source without modifying P1's package.
- Begin the writeup-only ZK proof only after the CredAttest source is supplied; do not put it on the listed-product path.

### Questions for humans

- Please provide the CredAttest EZKL pipeline path or repository URL.
- Please provide official-source-verified ERC-8004 registry addresses for Ethereum and Base, and confirm whether a registry exists on X Layer.
- `INTERFACES.md` fixes component weights but not each component's internal point rubric. Please approve or replace the documented rubric in `apps/kya/README.md` before listing.
- Please confirm that `check_agent` and `attest_agent` take the same `{agent_ref}` request as `get_flags`; section 2 only states the latter request explicitly.

---

## 2026-07-15 — G2 gate recheck

### Done

- Re-read `AGENTS.md`, `README.md`, `docs/INTERFACES.md`, and `docs/PLAN.md` in full and checked the latest P1 and DOCSYNC status before touching P2 code.
- Re-ran `pnpm --filter @constellation/kya test` (15/15) and `pnpm --filter @constellation/kya build`; both pass.
- Audited the three P2-local fixtures against the implemented weighted formula. The current component scores reconcile exactly after final rounding: `agent_good` 86.05 -> 86, `agent_transferred_identity` 67.3 -> 67, and `agent_sybil_burst` 49.2 -> 49.
- Confirmed current flags: `agent_good` has none; `agent_transferred_identity` has `IDENTITY_TRANSFERRED_RECENTLY`; `agent_sybil_burst` has both `REVIEWER_CONCENTRATION` and `BURST_FEEDBACK`. The previously reported missing burst flag is not present in the current P2 fixture.
- Confirmed the implementation currently gives `get_flags`, `check_agent`, and `attest_agent` the shared `AgentRequest` shape `{agent_ref}`. This is an implementation assumption, not a confirmed frozen-interface fact.

### Blocked

- G2 is not met. Treasury is live and registered, but P1 records Agent ID 5863 as not activated, the endpoint as still mock-charged, real x402 settlement as unproven, and real-wallet report hand-verification as open. No KYA live integration or listing work may start.
- The component point rubric in `apps/kya/README.md` still lacks both humans' sign-off. Passing fixtures do not resolve that product decision.
- `check_agent` and `attest_agent` request shapes remain ambiguous in `docs/INTERFACES.md`; only `get_flags` explicitly specifies `{agent_ref}`.
- Official-source-verified ERC-8004 registry addresses for Ethereum and Base, X Layer availability, and the CredAttest EZKL source remain unavailable. No values will be guessed.

### Next

- Keep P2 frozen while Treasury owns the critical path.
- If and only if P1 records that G2 passed with genuine slack, first obtain human sign-off on the rubric and request shape, then add an explicit all-fixture weighted-sum regression test and wire the read-only `packages/erc8004` evidence source without modifying P1's package.
- Keep ZK writeup-only and deferred; do not put it on the listed-product path.

### Questions for humans

- Poulav and Ishita: do you both approve the per-component point rubric documented in `apps/kya/README.md`? Please record two explicit yes/no answers.
- Do `check_agent` and `attest_agent` use the same `{agent_ref}` request shape as `get_flags`? Please confirm or version the frozen interface.
- When Treasury is approved and receiving real calls, please record an explicit G2 GO before P2 resumes.

---

## 2026-07-15 — legacy cleanup request

### Done

- Assessed the requested repository-wide LedgerForge/Celo cleanup against the active P2 ownership matrix.

### Blocked

- Repository-wide deletion crosses P2's absolute ownership boundary and the shared worktree contains active uncommitted P1 payment-adapter work. No legacy or Constellation product files were changed.

### Next

- If a human explicitly reassigns this session to a temporary cleanup workstream, inventory legacy-only paths in a separate worktree, present the deletion manifest, remove only approved paths, and verify the surviving Constellation workspace.

### Questions for humans

- Do you explicitly reassign this session from P2 to a repository-wide cleanup workstream authorized to edit/delete files outside the P2 ownership matrix while preserving all Constellation code and current P1 changes?
